import { NumberRounderUpPipe } from './numberRounderUp.pipe';

describe('NumberRounderUpPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberRounderUpPipe();
    expect(pipe).toBeTruthy();
  });
});
